---
import Layout from "../../../layouts/Layout.astro";
import PageWrapper from "../../../components/common/PageWrapper.astro";
import { EditCollection } from "../../../components/collections";

const { id } = Astro.params;
const safeId = id ?? "";
---

<Layout title={`Edytuj kolekcję`}>
  <PageWrapper title="Edytuj kolekcję" description="Zmień nazwę i opis swojej kolekcji fiszek">
    <EditCollection client:only="react" collectionId={safeId} />
  </PageWrapper>
</Layout>
