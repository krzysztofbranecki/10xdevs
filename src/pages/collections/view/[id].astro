---
import Layout from "../../../layouts/Layout.astro";
import PageWrapper from "../../../components/common/PageWrapper.astro";
import { CollectionView } from "../../../components/collections";

const { id } = Astro.params;
const safeId = id ?? "";
---

<Layout title={`Podgląd kolekcji`}>
  <PageWrapper title="Podgląd kolekcji" description="Szczegóły wybranej kolekcji fiszek">
    <CollectionView client:only="react" collectionId={safeId} />
  </PageWrapper>
</Layout>
